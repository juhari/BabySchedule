<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="fi.vincit.babyschedule"
      android:versionCode="1"
      android:versionName="1.0" >
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:debuggable="true">    
        <activity android:name=".MainTabWidget"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>  
        <activity android:name=".activities.EventMarkingList"
        		  android:label="@string/app_name">
     	</activity>      
        <activity android:name=".activities.SingleEventList"
        		  android:label="@string/single_action">
		</activity>
		<activity android:name=".activities.AllEventsList"
        		  android:label="@string/events_list">
		</activity>
		<activity android:name=".activities.EventAdder"
        		  android:label="@string/single_action">
		</activity>
		<activity android:name=".activities.EventEditor"
        		  android:label="@string/single_action">
		</activity>
		
    </application>
</manifest>